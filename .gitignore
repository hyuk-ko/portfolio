# Local preview config (contains machine-specific absolute paths)
.claude/

# Local preview server script (machine-specific paths)
serve.ps1

# Private resume notes / drafts — keep out of the public Pages repo
이력서_고도화_제안.txt
박준혁_이력서_최종.md
resume.md
resume_Proposed Plan.txt

# Private work data (resume plans, work-log sheets) — never commit
data/
